# 自动签到
自动签到是一个简单的脚本，用于自动签到。

## 安装
```bash
# Node 环境 19 以上
npm install
npm run install-browsers
```

## 配置
1. 配置文件为`checkin.json`和`checkout.json`，分别为签到和签出的配置文件。
2. crontab

## 运行
```bash
docker-compose up --build -d
```
